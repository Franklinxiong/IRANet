# RGB Glass Detection Project - dependencies
#
# Install with:
#   pip install -r requirements.txt
#
# It is recommended to create a fresh environment first:
#   conda create -n glass_detection python=3.10
#   conda activate glass_detection
#   pip install -r requirements.txt

# Deep learning framework
# On GPU machines, install a CUDA build of PyTorch first, for example:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
# then install the rest with:
#   pip install -r requirements.txt
# Tested with torch 2.8.0.
torch>=2.0.0
torchvision>=0.15.0

# Image processing
opencv-python>=4.8.0
scikit-image>=0.20.0
pillow>=10.0.0

# Scientific computing
numpy>=1.24.0
scipy>=1.10.0                  # needed by scikit-image

# Machine learning
scikit-learn>=1.3.0

# Auxiliary tools
# joblib       - installed automatically with scikit-learn, used for parallel eval in evaluation_fast.py
# pycocotools  - only needed by the COCO mask helper script (optional)
#                  install with:
#                    pip install pycocotools
#                  or on Windows:
#                    pip install pycocotools-windows
# pycocotools

# Plotting
matplotlib>=3.7.0

# Utilities
tqdm>=4.65.0                   # progress bars
einops>=0.6.0                  # tensor ops
packaging>=23.0                # version checks

# CRF refinement (optional, for post-processing)
# Install from source (the pip package has not been updated in a while):
#   git clone https://github.com/lucasb-eyer/pydensecrf.git
#   cd pydensecrf
#   python setup.py install
# If it is not installed the scripts skip CRF refinement automatically.
# pydensecrf

# In-project modules (no installation needed)
# pytorch_ssim/  - SSIM loss (local package)
# pytorch_iou/   - IoU loss (local package)
# lovasz_losses.py - Lovasz-Softmax loss

# NVIDIA Apex (optional, for mixed-precision training)
# apex          - requires a source build:
#   git clone https://github.com/NVIDIA/apex
#   cd apex
#   pip install -v --no-build-isolation --no-cache-dir ./
#   torch native AMP is built in and is used by default instead
